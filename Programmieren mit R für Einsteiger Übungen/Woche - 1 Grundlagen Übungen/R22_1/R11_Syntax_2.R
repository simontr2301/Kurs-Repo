codeoceanR::rt_score()

# A3 ----
# Erstelle die folgenden Objekte gemäß ihres Titels.
# Nutze Code, nicht eine manuell eingegebene Zahl.
drei_hoch_zwei <- 0
haelfte_von_pi <- 0
sechzig_fakultaet <- 0 # 60! = 60*59*58*...*2*1, siehe Zusatzmaterial
e_hoch_4 <- 0
zehner_logarithmus_von_200 <- 0 #  10 ^ x = 200
drei_hoch_zwei <- 3^2
haelfte_von_pi <- pi/2
sechzig_fakultaet <- factorial(60)
e_hoch_4 <- exp(4)
zehner_logarithmus_von_200 <- log10(200)
# A4 ----
# Welche Objekte liegen jetzt im Global Environment workspace vor?
# Statt das nur anzeigen zu lassen, soll das in einem Objekt gespeichert werden.
liste_der_objekte <- ls()



# A5 ----
# Ersetze die Nullen hinter den Begriffen mit der Ziffer der zugehörigen Erklärung unten. 
# Nicht alle Erklärungen gehören zu einem der gelisteten Fachbegriffe.
Objekt <- 7
Argument <- 5
Zuweisung <- 6
Befehl <- 2
Workspace <- 4
Operator <- 9
# 1 Arbeitsverzeichnis (Ordner) auf dem PC, in dem R Dateien liest / schreibt
# 2 Eine Funktion, die etwas ausführt
# 3 Eine selbst geschriebene Funktion mit geschweiften Klammern
# 4 R-interner Speicher mit allen Objekten
# 5 Eingabeparameter einer Funktion
# 6 Die Erstellung eines Objektes mit dem Operator <- 
# 7 Element/Instanz/Struktur der Programmierung, auch 'Variable', zB für Werte, Tabellen, statistische Modelle
# 8 Erstellung runder Klammern zum Aufruf einer Funktion
# 9 Arithmetische Anweisung (+,-,/,*,^), auch relational (<,==) oder logisch (&,!)


# Mache weiter in "R11_Syntax_3.R"